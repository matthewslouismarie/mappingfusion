{% macro errorList(errors) %}
{% if errors|length > 0 %}
<ul>
    {% for e in errors|keys %}
    {% if e == e|round() %}
    <li class="item">{{ errors[e] }}</li>
    {% endif %}
    {% endfor %}
</ul>
{% endif %}
{% endmacro %}