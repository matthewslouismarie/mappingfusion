{% extends 'base.html.twig' %}

{% block title %}Liste des tests{% endblock %}

{% block bodyContent %}
<a class="link" href="{{ app.router.generateUrl('manage_review') }}">Créer un nouveau test</a>
<ul>
    {% for r in reviews %}
    <li class="item">
        <a class="link" href="{{ app.router.generateUrl('manage_review', {id: r.id}) }}">Pour {{ r.playableId }} (note {{ r.rating}})</a>
    </li>
    {% endfor %}
</ul>
{% endblock %}