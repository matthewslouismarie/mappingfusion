{% extends 'base.html.twig' %}

{% block bodyContent %}
<h1 class="_h1">Gestion des mods</h1>

<a class="std-button _std" href="{{ app.router.generateUrl('admin/jeu') }}">Créer un nouveau mod</a>

<ul class="std-list _std">
    {% for p in playables %}
    <li class="item">
        <a class="std-link" href="{{ app.router.generateUrl('admin/jeu', {id: p.id}) }}">{{ p.name }}</a>
    </li>
    {% endfor %}
</ul>
{% endblock %}